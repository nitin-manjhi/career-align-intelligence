spring:
  datasource:
    url: jdbc:postgresql://localhost:5433/resumeDb?options=-c%20timezone=Asia/Kolkata
    username: postgres
    password: Srcem@123
  data:
    redis:
      host: localhost
      port: 6379
  kafka:
    bootstrap-servers: localhost:9092

ollama:
  api-key: ${OLLAMA_API_KEY:}
  base-url: ${OLLAMA_BASE_URL:https://ollama.com}
  model: ${OLLAMA_MODEL:minimax-m2.5:cloud}
