Extract all technical skills, frameworks, and tools from the Job Description.
Return ONLY a JSON object with a single key "skills" containing a list of strings.

Example:
{"skills": ["Java", "Spring Boot", "AWS"]}

JD:
{jdText}
